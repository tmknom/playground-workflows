name: Example
on:
  workflow_dispatch:

permissions: {}

defaults:
  run:
    shell: bash

jobs:
  git-push:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: tmknom/checkout-action@v1

      - name: Create
        run: |
          set -x
          date > test.txt

      - name: Git Push
        id: push
        uses: tmknom/git-push-action@b81547a5d78b2de8032553306c611e9ff5f98f12 # v0.1.0

      - name: View
        env:
          PUSHED: ${{ steps.push.outputs.pushed }}
          BRANCH: ${{ steps.push.outputs.branch }}
        run: |
          set -x
          echo "PUSHED=${PUSHED}"
          echo "BRANCH=${BRANCH}"
