name: Test action
on:
  pull_request:
defaults:
  run:
    shell: bash
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@8ade135a41bc03ea155e62e844d188df1ea18608

      - name: Bump version
        id: bump
        uses: ./
        with:
          version: "v1.2.3"
          level: "patch"

      - name: Run
        env:
          NEXT_VERSION: ${{ steps.bump.outputs.next-version }}
        run: echo "run=${NEXT_VERSION}"
