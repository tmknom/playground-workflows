name: "Labeler"
on:
  pull_request:

permissions:
  pull-requests: write

jobs:
  label:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/labeler@5682b7c5137476cceba78108d82b25ae7897cc7c # v3.0.1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
