name: Rename me
description: |
  Test super action.

  ## Usage

  Write usage for your Composite Action.

  ```yaml
    steps:
      - name: Template
        uses: tmknom/template-composite-action@v0
  ```

runs:
  using: composite

  steps:
    - name: Run
      shell: bash
      run: |
        set -x
        echo "Write steps"
